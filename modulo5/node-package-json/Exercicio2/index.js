// Exercicio 2

const n1 = Number(process.argv[3]);
const n2 = Number(process.argv[4]);

      function add(n1, n2) {
        return n1 + n2;
      }
      const respostaAdd = add(n1, n2);
      console.log(respostaAdd);


      function sub(n1, n2) {
        return n1 - n2;
      }
      const respostaSub = sub(n1, n2);
      console.log(respostaSub);


      function mult(n1, n2) {
        return n1 * n2;
      }
      const respostaMult = mult(n1, n2);
      console.log(respostaMult);
 

      function div(n1, n2) {
        return n1 / n2;
      }
      const respostaDiv = div(n1, n2);
      console.log(respostaDiv);
